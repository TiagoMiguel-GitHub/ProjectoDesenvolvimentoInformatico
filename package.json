{
  "name": "agrowood",
  "version": "1.0.0",
  "scripts": {
    "start": "start cmd /k \"cd apps/admin && npm run dev\" && cd apps/mobile && npx expo start --lan",
    "start:admin": "cd apps/admin && npm run dev",
    "start:mobile": "cd apps/mobile && npx expo start --lan"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
